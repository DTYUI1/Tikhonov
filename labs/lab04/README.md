# Лабораторная работа: Синхронизация потоков в C#

## 📋 Описание проекта

Проект демонстрирует классические проблемы синхронизации потоков в многопоточных приложениях и способы их решения. Реализованы три известные задачи из теории параллельного программирования: "Обедающие философы", "Спящий парикмахер" и "Производитель-Потребитель".

## 🎯 Цели работы

1. Изучить проблемы синхронизации потоков в многопоточных приложениях
2. Научиться выявлять и предотвращать взаимоблокировки (deadlock)
3. Освоить использование механизмов синхронизации в C#
4. Реализовать корректную работу с общими ресурсами
5. Изучить различия между различными типами блокировок

## 🏗️ Структура проекта

### 1. **Обедающие философы (Dining Philosophers)**

**Описание задачи:**
5 философов сидят за круглым столом, между ними лежат 5 вилок. Для приема пищи философу нужны две вилки (левая и правая). Философы могут думать и есть. Каждый философ представлен отдельным потоком.

**Реализованные версии:**
- **Версия с deadlock**: Демонстрация классической взаимоблокировки, когда все философы берут левую вилку и ждут правую
- **Версия без deadlock**: Решение проблемы с использованием SemaphoreSlim и изменения порядка взятия вилок

**Используемые механизмы:**
- `lock` для синхронизации доступа к вилкам
- `SemaphoreSlim` для ограничения количества одновременно питающихся философов
- `Thread` для представления каждого философа

### 2. **Спящий парикмахер (Sleeping Barber)**

**Описание задачи:**
Парикмахер спит, когда нет клиентов. Клиенты приходят в парикмахерскую и занимают места в ограниченной очереди. Если все места заняты, клиент уходит. Парикмахер просыпается, когда приходит клиент.

**Особенности реализации:**
- Ограниченная очередь клиентов (3 места)
- Парикмахер работает в отдельном потоке
- Клиенты генерируются случайным образом
- Синхронизация между парикмахером и клиентами

**Используемые механизмы:**
- `Mutex` для защиты критических секций
- `SemaphoreSlim` для синхронизации парикмахера и клиентов
- `Queue<int>` для представления очереди ожидания
- `lock` для потокобезопасной работы с очередью

### 3. **Производитель-Потребитель (Producer-Consumer)**

**Описание задачи:**
Производители создают элементы, потребители их забирают. Буфер ограниченного размера. Необходимо обеспечить корректную работу без переполнения буфера и чтения из пустого буфера.

**Реализованные решения:**
- **С использованием BlockingCollection**: Готовое решение из .NET с автоматической синхронизацией
- **С использованием SemaphoreSlim и lock**: Ручная реализация с двумя семафорами

**Особенности:**
- Ограниченный буфер (5 элементов)
- Несколько производителей и потребителей
- Синхронизация через семафоры для контроля свободных/занятых мест
- Потокобезопасный доступ к буферу

## 🛠️ Технические особенности

### Механизмы синхронизации

**lock (Monitor):**
```csharp
lock (syncObject)
{
    // Критическая секция
}
```
Используется для взаимного исключения в пределах одного процесса.

**SemaphoreSlim:**
```csharp
private SemaphoreSlim semaphore = new SemaphoreSlim(initialCount, maxCount);
```
Легковесный семафор для ограничения доступа к ресурсам.

**Mutex:**
```csharp
private Mutex mutex = new Mutex();
mutex.WaitOne();
mutex.ReleaseMutex();
```
Используется для синхронизации между потоками и процессами.

**BlockingCollection:**
```csharp
BlockingCollection<int> buffer = new BlockingCollection<int>(capacity);
```
Автоматически блокирующая коллекция для реализации шаблона Producer-Consumer.

### Потоки и их управление

**Создание потоков:**
```csharp
Thread thread = new Thread(() => MethodName(parameters));
thread.Start();
```

**Ожидание завершения:**
```csharp
thread.Join();
```

**Прерывание потоков:**
```csharp
thread.Interrupt();
```

## 📊 Примеры работы

### Обедающие философы
```
Версия с deadlock:
Философ 0 думает
Философ 0 хочет взять левую вилку 0
Философ 0 взял левую вилку 0
...
(deadlock возникает через несколько секунд)

Версия без deadlock:
Философ 0 думает
Философ 0 хочет взять левую вилку 0
Философ 0 взял левую вилку 0
Философ 0 хочет взять правую вилку 1
Философ 0 взял правую вилку 1
Философ 0 ест с вилками 0 и 1
```

### Спящий парикмахер
```
Парикмахер пришел на работу
Парикмахер спит
Клиент 1 вошел. Мест в очереди: 1
Парикмахер начал стричь клиента 1
Клиент 2 вошел. Мест в очереди: 1
Парикмахер закончил стричь клиента 1
Парикмахер начал стричь клиента 2
```

### Производитель-Потребитель
```
Решение с BlockingCollection:
Производитель 0 произвел: 0. В буфере: 1
Потребитель 0 потребил: 0. В буфере: 0
Производитель 1 произвел: 100. В буфере: 1
```

## 🚀 Запуск проектов

### Требования:
- .NET 5.0 или выше (или .NET Core 3.1)
- Любая среда разработки (Visual Studio, VS Code, Rider)

### Компиляция и запуск:

**Для каждой задачи отдельно:**
```bash
# Переход в папку задачи
cd task1

# Компиляция (выберите подходящий способ)
# Способ 1: dotnet (если создан проект)
dotnet build
dotnet run

# Способ 2: mcs (Mono)
mcs DiningPhilosophers.cs
mono DiningPhilosophers.exe

# Способ 3: csc (.NET Framework на Windows)
csc DiningPhilosophers.cs
DiningPhilosophers.exe
```

**Для задач 2 и 3 аналогично:**
```bash
cd task2
# компиляция и запуск

cd task3
# компиляция и запуск
```

## 📝 Ключевые концепции

### 1. **Deadlock (Взаимоблокировка)**
Ситуация, когда два или более потока заблокированы, ожидая ресурсы, занятые друг другом.
**Пример**: Философы ждут вилки, которые держат другие философы.

### 2. **Race Condition (Состояние гонки)**
Неопределенное поведение программы из-за неконтролируемого порядка выполнения потоков.
**Пример**: Несколько потоков пытаются одновременно изменить общую переменную.

### 3. **Критическая секция**
Участок кода, в котором происходит доступ к общим ресурсам, требующий синхронизации.

### 4. **Starvation (Голодание)**
Ситуация, когда поток долгое время не может получить доступ к ресурсу.

## 🔧 Основные решения проблем синхронизации

1. **Использование lock/Monitor** - для простых случаев взаимного исключения
2. **Семафоры** - для ограничения количества потоков, работающих с ресурсом
3. **Изменение порядка блокировок** - для предотвращения deadlock
4. **Автоматические коллекции** - BlockingCollection для Producer-Consumer
5. **Таймауты** - для предотвращения бесконечного ожидания

## 📚 Вопросы для защиты

### 1. Что такое race condition? Приведите пример из задач
**Ответ**: Race condition возникает, когда несколько потоков одновременно обращаются к общим данным без синхронизации. В задаче "Производитель-Потребитель" без синхронизации несколько потоков могли бы одновременно изменять индексы буфера.

### 2. Чем deadlock отличается от starvation?
**Ответ**: 
- **Deadlock** - взаимная блокировка потоков (никогда не продолжат выполнение)
- **Starvation** - поток долго ждет ресурс, но теоретически может его получить

### 3. Что такое критическая секция?
**Ответ**: Участок кода, в котором происходит доступ к общим ресурсам. В коде защищается оператором `lock`.

### 4. Объясните разницу:
- **lock и Monitor**: `lock` - синтаксический сахар для `Monitor.Enter/Monitor.Exit`
- **Mutex и SemaphoreSlim**: Mutex - для взаимного исключения, SemaphoreSlim - для ограничения доступа к ресурсу
- **Semaphore и SemaphoreSlim**: Semaphore - системный, для межпроцессного взаимодействия, SemaphoreSlim - легковесный, для внутрипроцессной синхронизации

## 🔍 Возможные улучшения

1. Добавить графический интерфейс для визуализации работы потоков
2. Реализовать дополнительные алгоритмы предотвращения deadlock
3. Добавить статистику работы потоков
4. Реализовать тесты для проверки корректности синхронизации
5. Добавить конфигурацию параметров через файлы

## 💡 Практическое применение

Полученные знания применяются в:
- Веб-серверах для обработки параллельных запросов
- Базах данных для управления транзакциями
- Игровых серверах
- Научных вычислениях
- Системах реального времени

## 📖 Дополнительные материалы

1. MSDN документация по многопоточности в .NET
2. Книга "Concurrency in C# Cookbook" by Stephen Cleary
3. Статьи о паттернах параллельного программирования
4. Документация по классам `System.Threading`

---

*Проект создан в учебных целях для изучения основ многопоточного программирования и синхронизации потоков в C#.*
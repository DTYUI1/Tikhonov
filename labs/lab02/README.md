# Лабораторная работа: Анализ производительности коллекций в C#

## 📋 Описание проекта

Проект представляет собой комплексный анализ производительности различных коллекций в .NET. Реализовано тестирование операций добавления, удаления, поиска и доступа по индексу для пяти типов коллекций с измерением времени выполнения и статистической обработкой результатов.

## 🎯 Цели работы

1. Сравнить производительность разных типов коллекций .NET
2. Проанализировать сложность операций для каждой коллекции
3. Реализовать методы измерения времени выполнения операций
4. Создать систему тестирования с многократными запусками для статистической достоверности
5. Разработать юнит-тесты для проверки корректности реализации

## 🏗️ Структура проекта

### 1. **Program.cs** - Основной модуль тестирования производительности

**Тестируемые коллекции:**
- `List<T>` - динамический массив
- `LinkedList<T>` - двусвязный список
- `Queue<T>` - очередь FIFO
- `Stack<T>` - стек LIFO
- `ImmutableList<T>` - неизменяемый список

**Тестируемые операции для каждой коллекции:**
- Добавление элементов (в конец, в начало, в середину)
- Удаление элементов (с конца, с начала, из середины)
- Поиск элементов
- Доступ по индексу (где применимо)

### 2. **CollectionPerformanceTests.cs** - Юнит-тесты

**Тестируемые аспекты:**
- Корректность работы метода измерения времени
- Правильность поиска среднего элемента в связанном списке
- Отсутствие исключений при выполнении всех тестов производительности

### 3. **Методы измерения производительности**

**Основные методы:**
- `MeasureTime()` - измерение времени выполнения операции
- `PrintResults()` - вывод статистики (среднее, минимум, максимум)
- `GetMiddleNode()` - поиск среднего элемента в связанном списке

## 📊 Алгоритмы тестирования

### Для каждой коллекции выполняются:

1. **Многократные запуски** (задаётся параметром `repeatTests`)
2. **Измерение времени** для каждой операции
3. **Статистическая обработка** результатов
4. **Сравнительный анализ** производительности

### Пример конфигурации:
```csharp
int elementsCount = 100000;    // Количество элементов
int repeatTests = 5;           // Количество повторений
```

## 🚀 Запуск проекта

### Требования:
- .NET 8.0 или выше
- Установленный xUnit для тестов

### Сборка и запуск тестов:
```bash
# Перейти в директорию проекта
cd /путь/к/проекту

# Запустить все тесты
dotnet test

# Запустить конкретный тестовый проект
dotnet test CollectionsPerf.Tests/CollectionsPerf.Tests.csproj

# Собрать и запустить основное приложение
dotnet run --project CollectionsPerf/CollectionsPerf.csproj
```

## 📊 Результаты тестирования производительности

### Ключевые выводы из тестирования:

#### **1. List<int> - Самая быстрая для большинства операций**
- **Добавление в конец**: 0.17 мс (O(1) амортизированно)
- **Добавление в начало**: 0.11 мс (быстрее, чем ожидалось для O(n))
- **Вставка в середину**: 4.28 мс (самая медленная операция, O(n))
- **Поиск элемента**: 6.97 мс (самая медленная операция, O(n))

#### **2. LinkedList<int> - Специализирован для определённых сценариев**
- **Все основные операции**: не поддерживаются или требуют O(n) времени
- **Быстрые операции**: добавление/удаление только с начала/конца
- **Ограничение**: нет доступа по индексу

#### **3. Queue<int> - Оптимизирован для FIFO**
- **Enqueue (добавление)**: 0.18 мс
- **Dequeue (удаление)**: 0.57 мс
- **Поиск**: 0.18 мс (неожиданно быстрый для O(n))

#### **4. Stack<int> - Оптимизирован для LIFO**
- **Push (добавление)**: 0.12 мс
- **Pop (удаление)**: 0.51 мс
- **Поиск**: 0.17 мс

#### **5. ImmutableList<int> - Самый медленный, но потокобезопасный**
- **Добавление в конец**: 2.09 мс (в 12 раз медленнее List)
- **Добавление в начало**: 4.72 мс (в 43 раза медленнее List)
- **Все операции**: создают новую коллекцию, поэтому медленнее

## 🎯 Рекомендации по выбору коллекции

### Выбирайте **List<T>**, когда:
- ✅ Нужен быстрый доступ по индексу
- ✅ Часто добавляете в конец коллекции
- ✅ Нужны самые быстрые операции в большинстве случаев
- ❌ Избегайте частых вставок/удалений в середину

### Выбирайте **LinkedList<T>**, когда:
- ✅ Частые вставки/удаления в произвольных позициях
- ✅ Не нужен доступ по индексу
- ✅ Работаете с большими данными, где копирование массива дорого
- ❌ Избегайте частых операций поиска

### Выбирайте **Queue<T>**, когда:
- ✅ Реализуете алгоритмы FIFO (очередь)
- ✅ Обрабатываете задачи в порядке поступления
- ✅ Нужна гарантированная порядковая обработка
- ❌ Не нужен доступ к элементам по индексу

### Выбирайте **Stack<T>**, когда:
- ✅ Реализуете алгоритмы LIFO (стек)
- ✅ Обрабатываете вызовы функций/рекурсию
- ✅ Нужен механизм отмены операций
- ❌ Не нужен произвольный доступ к элементам

### Выбирайте **ImmutableList<T>**, когда:
- ✅ Работаете в многопоточном окружении
- ✅ Нужна гарантия неизменяемости данных
- ✅ Реализуете функциональный подход
- ❌ Избегайте в высокопроизводительных сценариях

## 🔬 Особенности реализации

### 1. **Метод MeasureTime()**
```csharp
private static TimeSpan MeasureTime(Action action)
{
    var stopwatch = Stopwatch.StartNew();
    action();
    stopwatch.Stop();
    return stopwatch.Elapsed;
}
```
- Использует высокоточный таймер `Stopwatch`
- Минимизирует накладные расходы на измерение

### 2. **Алгоритм поиска среднего элемента (Two Pointers)**
```csharp
private static LinkedListNode<int> GetMiddleNode(LinkedList<int> list)
{
    if (list.Count == 0) return null;
    
    var slow = list.First;
    var fast = list.First;
    
    while (fast?.Next != null && fast.Next.Next != null)
    {
        slow = slow!.Next;
        fast = fast.Next.Next;
    }
    
    return slow;
}
```
- Алгоритм "быстрый и медленный указатель"
- Сложность O(n) при одном проходе
- Для чётного количества элементов возвращает "левую" середину

### 3. **Обработка ImmutableList**
```csharp
// Каждая операция создаёт новую коллекцию
var immutableList = ImmutableList<int>.Empty;
for (int i = 0; i < elementsCount; i++)
{
    immutableList = immutableList.Add(i); // Новый экземпляр
}
```
- Демонстрация отличий изменяемых и неизменяемых коллекций
- Более высокие накладные расходы на операции

## 🔍 Анализ сложности операций (фактические измерения)

| Операция | List<T> | LinkedList<T> | Queue<T> | Stack<T> | ImmutableList<T> |
|----------|---------|---------------|----------|----------|------------------|
| **Добавление в конец** | O(1)* | O(1) | O(1) | - | O(n) |
| **Добавление в начало** | O(n) | O(1) | - | O(1) | O(n) |
| **Вставка в середину** | O(n) | O(n) | - | - | O(n) |
| **Удаление с конца** | O(1) | O(1) | - | O(1) | O(n) |
| **Удаление с начала** | O(n) | O(1) | O(1) | - | O(n) |
| **Поиск элемента** | O(n) | O(n) | O(n) | O(n) | O(n) |
| **Доступ по индексу** | O(1) | - | - | - | O(log n) |

*Примечание: O(1) амортизированно для List<T> при добавлении в конец*

## 📈 Тенденции производительности

1. **Самые быстрые операции**: 
   - Добавление в начало/конец LinkedList (0.06-0.11 мс)
   - Доступ по индексу List (0.04 мс)
   - Операции Stack/Push (0.12 мс)

2. **Самые медленные операции**:
   - Вставка в середину List (4.28 мс)
   - Поиск в List (6.97 мс)
   - Добавление в начало ImmutableList (4.72 мс)

3. **Неожиданные результаты**:
   - Queue поиск быстрее, чем ожидалось (0.18 мс)
   - List добавление в начало быстрее, чем ожидалось для O(n) (0.11 мс)

## 🧪 Юнит-тестирование

### Тест-кейсы:
1. **MeasureTime_ВозвращаетНеотрицательноеВремя** - проверка корректности измерения
2. **GetMiddleNode_КорректноИщетСередину** - проверка алгоритма поиска
3. **Test[Collection]_ОтрабатываетБезИсключений** - проверка стабильности всех тестов

### Особенности тестов:
- Использование рефлексии для доступа к приватным методам
- Тестирование граничных случаев (пустые коллекции)
- Проверка корректности типов возвращаемых значений

## 🔧 Настройка параметров тестирования

### Основные параметры (Program.cs):
```csharp
int elementsCount = 100000;    // Можно изменять для разных объёмов данных
int repeatTests = 5;           // Количество повторений для статистики
```

### Параметры для тестов (CollectionPerformanceTests.cs):
```csharp
// Для быстрых коллекций
InvokePrivateTestMethod("TestList", elementsCount: 1000, repeatTests: 2);

// Для медленных коллекций
InvokePrivateTestMethod("TestImmutableList", elementsCount: 500, repeatTests: 1);
```

## 🚀 Практические рекомендации

### Для высокопроизводительных приложений:
```csharp
// Используйте List<T> для большинства сценариев
var list = new List<int>(); // Быстрый, универсальный

// Используйте специализированные коллекции для конкретных задач
var queue = new Queue<int>(); // Для FIFO
var stack = new Stack<int>(); // Для LIFO
```

### Для многопоточных приложений:
```csharp
// Используйте ImmutableList<T> для безопасности
var immutableList = ImmutableList<int>.Empty;
// Или используйте потокобезопасные обёртки
var concurrentList = new List<int>();
// с использованием lock в многопоточной среде
```

### Оптимизация производительности:
1. **Предварительное выделение памяти** для List:
   ```csharp
   var list = new List<int>(capacity: 100000);
   ```

2. **Избегайте частых вставок в середину** больших List

3. **Используйте HashSet/Dictionary** для быстрого поиска

## 📊 Детальная статистика по коллекциям

### List<int> (самый быстрый в большинстве тестов)
| Операция | Среднее | Минимум | Максимум | Тестов |
|----------|---------|---------|----------|--------|
| Добавление в конец | 0.17 мс | 0.05 мс | 0.29 мс | 2 |
| Добавление в начало | 0.11 мс | 0.05 мс | 0.18 мс | 2 |
| Вставка в середину | 4.28 мс | 3.72 мс | 4.84 мс | 2 |
| Удаление с конца | 0.45 мс | 0.06 мс | 0.84 мс | 2 |
| Удаление с начала | 0.11 мс | 0.06 мс | 0.17 мс | 2 |
| Удаление из середины | 3.84 мс | 3.75 мс | 3.93 мс | 2 |
| Поиск элемента | 6.97 мс | 6.51 мс | 7.43 мс | 2 |

### ImmutableList<int> (самый медленный, но потокобезопасный)
| Операция | Среднее | Минимум | Максимум | Тестов |
|----------|---------|---------|----------|--------|
| Добавление в конец | 2.09 мс | 2.09 мс | 2.09 мс | 1 |
| Добавление в начало | 4.72 мс | 4.72 мс | 4.72 мс | 1 |
| Вставка в середину | 0.72 мс | 0.72 мс | 0.72 мс | 1 |
| Удаление с конца | 0.78 мс | 0.78 мс | 0.78 мс | 1 |
| Удаление с начала | 0.19 мс | 0.19 мс | 0.19 мс | 1 |
| Удаление из середины | 0.21 мс | 0.21 мс | 0.21 мс | 1 |
| Поиск элемента | 1.25 мс | 1.25 мс | 1.25 мс | 1 |
| Доступ по индексу | 0.32 мс | 0.32 мс | 0.32 мс | 1 |

## 📝 Ключевые выводы из лабораторной

1. **Выбор коллекции зависит от операций**:
   - `List<T>` - когда нужен быстрый доступ по индексу
   - `LinkedList<T>` - частые вставки/удаления в середине
   - `Queue<T>/Stack<T>` - специализированные сценарии FIFO/LIFO
   - `ImmutableList<T>` - многопоточные приложения

2. **Производительность vs удобство**:
   - Самые быстрые операции не всегда самые удобные
   - Immutable коллекции безопаснее, но медленнее

3. **Важность статистики**:
   - Однократное измерение может быть неточным
   - Среднее значение по нескольким запускам даёт более объективную картину

## 🚀 Возможные расширения

1. Добавить тестирование других коллекций:
   - `Dictionary<TKey, TValue>`
   - `HashSet<T>`
   - `SortedList<TKey, TValue>`

2. Реализовать сравнительные графики производительности

3. Добавить тестирование памяти (использование RAM)

4. Реализовать многопоточное тестирование

5. Создать веб-интерфейс для настройки параметров тестирования

## 📚 Используемые технологии и концепции

- **.NET 8.0** - платформа выполнения
- **xUnit** - фреймворк для тестирования
- **System.Collections** - пространство имён для коллекций
- **System.Collections.Immutable** - неизменяемые коллекции
- **System.Diagnostics.Stopwatch** - точное измерение времени
- **Reflection** - доступ к приватным методам в тестах
- **Алгоритм Two Pointers** - поиск среднего элемента
- **Статистическая обработка** - анализ результатов тестирования

---

*Лабораторная работа разработана для изучения особенностей производительности различных типов коллекций в .NET и выработки навыков выбора оптимальной структуры данных для конкретной задачи.*

*Результаты тестирования показывают, что выбор коллекции должен основываться на конкретных операциях, которые будут выполняться наиболее часто. List<T> остаётся оптимальным выбором для большинства сценариев, в то время как специализированные коллекции предоставляют преимущества в специфических случаях использования.*
# Лабораторная работа: Реализация пользовательских коллекций в C#

## 📋 Описание проекта

Проект представляет собой реализацию двух пользовательских коллекций: `MyList<T>` и `MyDictionary<TKey, TValue>` с нуля. Обе коллекции полностью реализуют стандартные интерфейсы .NET и предоставляют функциональность, аналогичную встроенным `List<T>` и `Dictionary<TKey, TValue>`.

## 🎯 Цели работы

1. Реализовать динамический список `MyList<T>` с поддержкой автоматического расширения
2. Реализовать хэш-таблицу `MyDictionary<TKey, TValue>` с разрешением коллизий методом цепочек
3. Обеспечить полную совместимость со стандартными интерфейсами .NET
4. Протестировать корректность работы всех операций
5. Продемонстрировать интеграцию пользовательских коллекций

## 🏗️ Архитектура проекта

### Структура файлов:
```
CustomCollections/
├── Program.cs          # Демонстрация работы коллекций
├── MyList.cs          # Реализация динамического списка
├── MyDictionary.cs    # Реализация хэш-таблицы
└── CustomCollections.csproj

CustomCollections.Tests/
├── MyListTests.cs     # Юнит-тесты для MyList
├── MyDictionaryTests.cs # Юнит-тесты для MyDictionary
└── CustomCollections.Tests.csproj
```

## 📚 Реализованные коллекции

### 1. **MyList<T> - Динамический массив**

#### Особенности реализации:
- **Автоматическое расширение**: при заполнении массива емкость удваивается
- **Эффективные операции**: O(1) доступ по индексу, O(n) вставка/удаление
- **Полная поддержка интерфейсов**: `IList<T>`, `ICollection<T>`, `IEnumerable<T>`

#### Ключевые методы:
```csharp
public void Add(T item)              // Добавление в конец
public void Insert(int index, T item) // Вставка по индексу
public bool Remove(T item)           // Удаление элемента
public T this[int index]             // Индексатор
public int IndexOf(T item)           // Поиск элемента
```

#### Внутренняя структура:
```csharp
private T[] _items;    // Внутренний массив
private int _size;     // Количество элементов
private int _version;  // Версия для контроля изменений
```

### 2. **MyDictionary<TKey, TValue> - Хэш-таблица**

#### Особенности реализации:
- **Метод цепочек**: разрешение коллизий через связанные списки в массиве
- **Автоматическое рехеширование**: при заполнении 75% емкости
- **Эффективный поиск**: O(1) в среднем случае

#### Структура данных:
```csharp
private struct Entry
{
    public int HashCode;  // Хэш ключа
    public int Next;      // Индекс следующего элемента в цепочке
    public TKey Key;      // Ключ
    public TValue Value;  // Значение
}

private int[] _buckets;   // Массив корзин
private Entry[] _entries; // Массив записей
```

#### Ключевые методы:
```csharp
public void Add(TKey key, TValue value)    // Добавление пары
public bool Remove(TKey key)               // Удаление по ключу
public TValue this[TKey key]               // Индексатор
public bool TryGetValue(TKey key, out TValue value) // Безопасное получение
```

## 🔧 Технические детали реализации

### MyList<T>:
```csharp
// Увеличение емкости при необходимости
private void EnsureCapacity(int min)
{
    if (_items.Length < min)
    {
        int newCapacity = _items.Length == 0 ? DefaultCapacity : _items.Length * 2;
        if (newCapacity < min) newCapacity = min;
        Array.Resize(ref _items, newCapacity);
    }
}
```

### MyDictionary<TKey, TValue>:
```csharp
// Поиск записи в хэш-таблице
private int FindEntry(TKey key)
{
    if (_buckets == null) return -1;
    
    int hashCode = key.GetHashCode() & 0x7FFFFFFF; // Положительный хэш
    
    for (int i = _buckets[hashCode % _buckets.Length]; i >= 0; i = _entries[i].Next)
    {
        if (_entries[i].HashCode == hashCode && 
            EqualityComparer<TKey>.Default.Equals(_entries[i].Key, key))
            return i;
    }
    return -1;
}
```

## 🧪 Тестирование

### Юнит-тесты:

#### Для MyList<T>:
- Добавление и доступ по индексу
- Вставка и удаление элементов
- Поиск и проверка существования
- Копирование в массив
- Обработка граничных случаев

#### Для MyDictionary<TKey, TValue>:
- Добавление и обновление пар
- Удаление по ключу и паре ключ-значение
- Работа с коллизиями хэшей
- Получение ключей и значений
- Копирование в массив

### Пример теста:
```csharp
[Fact]
public void Add_And_Indexer_WorkCorrectly()
{
    var list = new MyList<int>();
    list.Add(10);
    list.Add(20);
    list.Add(30);
    
    Assert.Equal(3, list.Count);
    Assert.Equal(10, list[0]);
    Assert.Equal(20, list[1]);
    Assert.Equal(30, list[2]);
}
```

## 🚀 Запуск проекта

### Требования:
- .NET 8.0 или выше
- xUnit для тестирования

### Команды:

```bash
# Сборка основного проекта
dotnet build CustomCollections/CustomCollections.csproj

# Запуск демонстрации
dotnet run --project CustomCollections/CustomCollections.csproj

# Запуск тестов
dotnet test CustomCollections.Tests/CustomCollections.Tests.csproj

# Быстрый запуск тестов (без пересборки)
dotnet test --no-build --verbosity normal
```

## 📊 Производительность

### MyList<T>:
| Операция | Сложность | Примечания |
|----------|-----------|------------|
| Доступ по индексу | O(1) | Прямой доступ к массиву |
| Добавление в конец | O(1)* | Амортизированная константа |
| Вставка в середину | O(n) | Требует сдвига элементов |
| Удаление | O(n) | Требует сдвига элементов |
| Поиск | O(n) | Линейный поиск |

### MyDictionary<TKey, TValue>:
| Операция | Сложность | Примечания |
|----------|-----------|------------|
| Добавление | O(1)* | Амортизированная константа |
| Поиск | O(1)* | В среднем случае |
| Удаление | O(1)* | В среднем случае |
| Обход всех элементов | O(n) | Все элементы |

*Примечание: O(1) - в среднем случае, зависит от распределения хэшей*

## 🔍 Особенности реализации

### 1. **Управление версиями (`_version`)**
```csharp
// Используется для контроля изменений коллекции
// Защищает от модификации во время итерации
private int _version;
```

### 2. **Свободные записи в MyDictionary**
```csharp
// Эффективное переиспользование удаленных записей
private int _freeList;    // Индекс первой свободной записи
private int _freeCount;   // Количество свободных записей
```

### 3. **Метод цепочек для разрешения коллизий**
```csharp
// Каждая запись содержит ссылку на следующую в цепочке
public struct Entry
{
    public int Next;  // Индекс следующего элемента (-1 если последний)
    // ...
}
```

## 🎯 Демонстрационные сценарии

### Сценарий 1: Базовые операции
```csharp
var list = new MyList<int>();
list.Add(10);
list.Add(20);
list.Add(30);
// Результат: [10, 20, 30]
```

### Сценарий 2: Интеграция коллекций
```csharp
var dict = new MyDictionary<string, MyList<int>>();
var numbers = new MyList<int> { 1, 2, 3 };
dict["numbers"] = numbers;
// Доступ: dict["numbers"][1] == 2
```

## 📈 Сравнение со стандартными коллекциями

| Функция | MyList<T> | List<T> | MyDictionary<K,V> | Dictionary<K,V> |
|---------|-----------|---------|-------------------|-----------------|
| Автоматическое расширение | ✅ | ✅ | ✅ | ✅ |
| Метод разрешения коллизий | - | - | Цепочки | Цепочки |
| Доступ по индексу | O(1) | O(1) | - | - |
| Поиск по значению | O(n) | O(n) | - | - |
| Поиск по ключу | - | - | O(1)* | O(1)* |


## 📚 Ключевые концепции

- **Динамические массивы**: автоматическое изменение размера
- **Хэш-таблицы**: эффективное хранение пар ключ-значение
- **Метод цепочек**: разрешение коллизий в хэш-таблицах
- **Амортизированный анализ**: оценка сложности операций
- **Интерфейсы .NET**: совместимость с экосистемой

## 💡 Практическое применение

1. **Образовательные цели**: понимание внутреннего устройства коллекций
2. **Специализированные коллекции**: адаптация под конкретные нужды
3. **Ограниченные среды**: где недоступны стандартные библиотеки
4. **Оптимизация производительности**: тонкая настройка под специфические сценарии

---

*Проект демонстрирует принципы реализации фундаментальных структур данных и их интеграцию в экосистему .NET. Реализованные коллекции могут использоваться как основа для более сложных структур данных и алгоритмов.*